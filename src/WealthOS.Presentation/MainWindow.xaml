<Window x:Class="WealthOS.Presentation.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:WealthOS.Presentation.ViewModels"
        xmlns:views="clr-namespace:WealthOS.Presentation.Views"
        mc:Ignorable="d"
        Title="{DynamicResource App.Title}"
        Height="900" Width="1400"
        MinHeight="700" MinWidth="1100"
        WindowStartupLocation="CenterScreen"
        Background="{StaticResource BackgroundBrush}">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Theme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="240"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="24,28,24,32">
                    <TextBlock Text="WealthOS" FontSize="22" FontWeight="Bold"
                               Foreground="White" Margin="0,0,0,4"/>
                    <TextBlock Text="Personal Wealth Operating System"
                               FontSize="11" Foreground="{StaticResource SidebarTextBrush}"/>
                </StackPanel>

                <StackPanel Grid.Row="1">
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Dashboard"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Left" Padding="0">
                        <Border CornerRadius="10" Padding="20,12" Margin="8,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsDashboardSelected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsDashboardSelected}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SidebarActiveTextBrush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}&#x1F3E0;  {0}">
                                        <Binding Source="{DynamicResource Nav.Dashboard}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </Button>
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Assets"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Left" Padding="0">
                        <Border CornerRadius="10" Padding="20,12" Margin="8,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAssetsSelected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAssetsSelected}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SidebarActiveTextBrush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}&#x1F4B0;  {0}">
                                        <Binding Source="{DynamicResource Nav.Assets}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </Button>
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Liabilities"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Left" Padding="0">
                        <Border CornerRadius="10" Padding="20,12" Margin="8,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsLiabilitiesSelected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsLiabilitiesSelected}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SidebarActiveTextBrush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}&#x1F4C9;  {0}">
                                        <Binding Source="{DynamicResource Nav.Liabilities}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </Button>
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Transactions"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Left" Padding="0">
                        <Border CornerRadius="10" Padding="20,12" Margin="8,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsTransactionsSelected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsTransactionsSelected}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SidebarActiveTextBrush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}&#x1F4CA;  {0}">
                                        <Binding Source="{DynamicResource Nav.Transactions}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </Button>
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Goals"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Left" Padding="0">
                        <Border CornerRadius="10" Padding="20,12" Margin="8,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsGoalsSelected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsGoalsSelected}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SidebarActiveTextBrush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}&#x1F3AF;  {0}">
                                        <Binding Source="{DynamicResource Nav.Goals}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </Button>
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Accounts"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Left" Padding="0">
                        <Border CornerRadius="10" Padding="20,12" Margin="8,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsAccountsSelected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsAccountsSelected}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SidebarActiveTextBrush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}&#x1F3E6;  {0}">
                                        <Binding Source="{DynamicResource Nav.Accounts}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </Button>
                    <Button Command="{Binding NavigateToCommand}" CommandParameter="Investments"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Left" Padding="0">
                        <Border CornerRadius="10" Padding="20,12" Margin="8,2">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsInvestmentsSelected}" Value="True">
                                            <Setter Property="Background" Value="{StaticResource SidebarActiveBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock FontSize="14">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{StaticResource SidebarTextBrush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsInvestmentsSelected}" Value="True">
                                                <Setter Property="Foreground" Value="{StaticResource SidebarActiveTextBrush}"/>
                                                <Setter Property="FontWeight" Value="SemiBold"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                                <TextBlock.Text>
                                    <MultiBinding StringFormat="{}&#x1F4C8;  {0}">
                                        <Binding Source="{DynamicResource Nav.Investments}"/>
                                    </MultiBinding>
                                </TextBlock.Text>
                            </TextBlock>
                        </Border>
                    </Button>
                </StackPanel>

                <StackPanel Grid.Row="2" Margin="24,0,24,24">
                    <Button Command="{Binding ToggleLanguageCommand}"
                            Background="Transparent" BorderThickness="0" Cursor="Hand"
                            HorizontalContentAlignment="Stretch" Padding="0" Margin="0,0,0,8">
                        <Border Background="{StaticResource SidebarActiveBrush}" CornerRadius="12" Padding="16,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{DynamicResource Lang.Current}" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource SidebarActiveTextBrush}"/>
                                <TextBlock Grid.Column="1" Text="{DynamicResource Lang.Switch}" FontSize="11"
                                           Foreground="{StaticResource SidebarTextBrush}"/>
                            </Grid>
                        </Border>
                    </Button>
                    <Border Background="{StaticResource SidebarActiveBrush}" CornerRadius="12" Padding="16,12">
                        <TextBlock Text="{DynamicResource App.Version}" FontSize="11"
                                   Foreground="{StaticResource SidebarTextBrush}"/>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <ContentControl Grid.Column="1"
                        Content="{Binding Navigation.CurrentViewModel}">
            <ContentControl.Resources>
                <DataTemplate DataType="{x:Type vm:DashboardViewModel}">
                    <views:DashboardView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:AssetsViewModel}">
                    <views:AssetsView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:LiabilitiesViewModel}">
                    <views:LiabilitiesView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:TransactionsViewModel}">
                    <views:TransactionsView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:GoalsViewModel}">
                    <views:GoalsView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:AccountsViewModel}">
                    <views:AccountsView/>
                </DataTemplate>
                <DataTemplate DataType="{x:Type vm:InvestmentsViewModel}">
                    <views:InvestmentsView/>
                </DataTemplate>
            </ContentControl.Resources>
        </ContentControl>
    </Grid>
</Window>
