<Window x:Class="WealthOS.Presentation.Views.StartupErrorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="WealthOS - 启动错误"
        Height="400" Width="600"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="&#x26A0;" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,16"
                   Foreground="#F59E0B"/>
        <TextBlock Grid.Row="1" Text="应用启动失败" FontSize="20" FontWeight="Bold"
                   HorizontalAlignment="Center" Foreground="#1E293B" Margin="0,0,0,8"/>
        <TextBlock Grid.Row="1" Text="请查看下方错误信息" FontSize="14"
                   HorizontalAlignment="Center" Foreground="#64748B" Margin="0,30,0,16"/>

        <Border Grid.Row="2" Background="#F1F5F9" CornerRadius="8" Padding="16" Margin="0,0,0,16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="ErrorTextBlock" TextWrapping="Wrap"
                           Foreground="#64748B" FontFamily="Consolas" FontSize="12"/>
            </ScrollViewer>
        </Border>

        <Button Grid.Row="3" Content="关闭" HorizontalAlignment="Center" Padding="32,10"
                Background="#3B82F6" Foreground="White" BorderThickness="0" Cursor="Hand"
                Click="CloseButton_Click">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}" CornerRadius="8"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</Window>
